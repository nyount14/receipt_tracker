<div class="text-center mt-4"></div>

<%= render 'budgets/budget', obj: @user.budget %>

<h1 class="text-center mt-5"><%= @user.username %>'s Transactions</h1>

<%= render 'transactions/transaction' %>