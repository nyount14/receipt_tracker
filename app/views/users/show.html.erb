<h1 class="text-center mt-4"><%= @user.username %>'s Profile</h1>

<%= gravatar_for @user, size: 200 %>

<h3 class="text-center mt-4"><%= @user.username %>'s Transactions</h3>

<%= render 'transactions/transaction' %>